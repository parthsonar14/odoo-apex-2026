I have a React app "transitops-frontend" for "TransitOps - Smart Transport 
Operations Platform" with a working Login page (src/pages/Login.jsx) 
connected to POST /api/auth/login.

My backend already has a working Signup API:
POST /api/auth/signup - accepts { full_name, email, password, role_id, phone }
Returns a success message on successful signup.

The Roles table has these role_ids: 1 = Fleet Manager, 2 = Dispatcher, 
3 = Safety Officer, 4 = Financial Analyst.

Now create a Registration/Signup page, keeping the existing file structure, 
coding style, and Tailwind design exactly consistent with the Login page:

1. Create src/pages/Register.jsx with a form matching the Login page's 
   visual style (centered card layout):
   - Fields: Full Name, Email, Password, Confirm Password, Phone, and a 
     Role dropdown (Fleet Manager, Dispatcher, Safety Officer, Financial Analyst)
   - Client-side validation: all fields required, password and confirm 
     password must match, basic email format check
   - On submit, call POST /api/auth/signup with the form data (map the 
     selected role label to its role_id)
   - Show loading state on the submit button while request is in progress
   - On success: show a success message and redirect to /login (do not 
     auto-login, since signup API only returns a success message, not a token)
   - On failure (e.g., email already exists): show a clear inline error 
     message (not alert)

2. Add a route in App.js: "/register" -> Register page (do not wrap it in 
   ProtectedRoute, same as Login)

3. On the Login page, add a small link/text at the bottom: "Don't have an 
   account? Register here" linking to /register

4. On the Register page, add a link at the bottom: "Already have an account? 
   Login here" linking to /login

CODE QUALITY REQUIREMENTS (STRICT):
- Do NOT change existing file structure or the working Login page logic
- Keep code clean, minimal, no unnecessary comments, no AI-style boilerplate 
  text - write exactly like a normal developer already working on this codebase
- Match the exact same Tailwind styling, spacing, and visual design already 
  used in the Login page for full consistency

Do not touch any other page or functionality - only create the Register page 
and link it to/from the Login page as described above.